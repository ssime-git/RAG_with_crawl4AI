# Required: Google API key for Gemini 2 Flash model
GOOGLE_API_KEY=your_google_api_key_here

# Optional: LiteLLM master key for API authentication (if needed)
# LITELLM_MASTER_KEY=your_litellm_master_key_here

# Optional: Model name (defaults to gemini-2.0-flash if not specified)
# MODEL_NAME=gemini-2.0-flash

# Optional: Path to LiteLLM config file (defaults to ./litellm_config.yaml)
# LITELLM_CONFIG=./litellm_config.yaml

# Optional: ChromaDB directory (defaults to ./chroma_db)
# DB_DIR=./chroma_db

# Optional: Collection name (defaults to docs)
# COLLECTION_NAME=docs
